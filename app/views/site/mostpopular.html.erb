<h2>Most Popular</h2>
<div class="row catalog">

<% @apps.each do|app| %>
  
<div class="col-sm-3 col-md-2 col-xs-6">
   <div style="margin:10px 0px;border-radius:10px;border:1px solid #aaa;background:#fff;box-shadow: 5px 5px 5px 0px #aaa;">
      <div class ="image" style="text-align:center">
        <%= link_to (image_tag app.image_url, :style =>"width:80%;border-radius:10px;margin:15px"), app %>
      </div>

      
        <div class="title" style="text-align:center">
          <h5><%= link_to app.title, app %></h5>
        </div>
        
        <div class="price" style="text-align:center">
          <h5>Downloads: <%= app.downloads%></h5>
        </div>
        
        <div class="price" style="text-align:center">
          <h5><%= number_to_currency(app.price, :unit =>"€") %></h5>
        </div>
        
        <div class="links"  style="text-align:center">
          <% if user_signed_in? %>
             <a href="/cart/<%=app.id%>">Add to Cart</a>
          <%else%>
          <%end %>
        </div>
          
          
        </div>

    </div>

<%end%>

</div>

<!--<p><%= link_to 'New app', new_app_path %></p>-->























<!--<p id="notice"><%= notice %></p>

<h1>Listing apps</h1>

<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Description</th>
      <th>Price</th>
      <th>Image url</th>
      <th>Category</th>
      <th>Platform</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @apps.each do |app| %>
      <tr>
        <td><%= app.title %></td>
        <td><%= app.description %></td>
        <td><%= app.price %></td>
        <td><%= app.image_url %></td>
        <td><%= app.category %></td>
        <td><%= app.platform %></td>
        <td><%= link_to 'Show', app %></td>
        <td><%= link_to 'Edit', edit_app_path(app) %></td>
        <td><%= link_to 'Destroy', app, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New app', new_app_path %>
-->